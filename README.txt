Git workflow has three steps:
1. Create the file
2. Stage thhe file   $ git add . (every file will get staged)
		     $ git add README.txt  $ git add *.txt
3. Commit the file  $ git commit -m "Initial commit and create two files"
